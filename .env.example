# Copy to `.env` (gitignored) and fill in. Read by src/utils/db.py.
# Standard libpq variable names, so psql picks them up too.
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=ravenstack
